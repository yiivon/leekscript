#ifndef UTIL_HPP_
#define UTIL_HPP_

#include <string>
#include <vector>
#include <queue>

class Util {
public:

	static std::string read_file(std::string file);
	static bool is_file_name(std::string data);
};

#endif
