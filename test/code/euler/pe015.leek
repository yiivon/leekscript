let S = 20

var paths = [0]
paths.fill(0, 20)

function walk(x, y) {
	if (x + y == S) {
		paths[y] = paths[y] + 1
		return
	}
	if (x > 0) walk(x - 1, y)
	if (y > 0) walk(x, y - 1)
	return
}

walk(S, S);

var sum = 1l
for (var i = 0; i < S; ++i) {
	sum += paths[i] ** 2
}
return sum
