/*

var singles = []
var groups = ['string' : 12] groups.erase('string')
var i = 405

while ++i {

    var n = ('' + i.pow(3)).sort()

	if n in groups {
		groups[n]++
		System.print(groups[n])
	} else {
		groups.insert(n, 1)
	}
}
